# Local development — points to Django running on localhost
VITE_API_URL=http://localhost:8000

# Production (Vercel) — set this to your Render backend URL:
# VITE_API_URL=https://instafood-backend.onrender.com
